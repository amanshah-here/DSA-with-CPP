#include<iostream>
#include<string>
using namespace std;

int main() {
    string str = "Hello";
    cout << str << endl;

    str = "Yellow"; //? Strings are dynamic that means its size can be changed at runtime.
    cout << str << endl;

    return 0;
}